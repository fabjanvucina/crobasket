import React from "react";
import { Link } from "@reach/router"; //eslint-disable-line
import "../style/City.css";

const City = (/* { name, imgSrc } */) => {
  return <div className="city"></div>;
  /* return <img className="city" src={imgSrc} alt={name}></img>; */
};

export default City;
